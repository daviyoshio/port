/**
 * Shared easing curve used across the site's animations.
 * Typed as a 4-number tuple so it satisfies framer-motion's BezierDefinition.
 */
export const EASE: [number, number, number, number] = [0.22, 1, 0.36, 1];
